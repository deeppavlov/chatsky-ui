build:
  # Backend build
  # - buildpacks:
  #     - heroku/python
  #   commands:
  #     - cd backend && pip install -r requirements.txt

  # Frontend build
  - buildpacks:
      - heroku/nodejs
    commands:
      - cd frontend && npm install

run:
  web: |
    cd frontend && npm start